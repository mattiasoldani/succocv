\moderncvstyle{classic}
% CV theme - moderncv options include: 'casual' (default), 'classic', 'oldstyle' and 'banking', but in succocv only the classic style is supported

\moderncvcolor{blue}
% CV color - options include: 'blue' (default), 'orange', 'green', 'red', 'purple', 'grey' and 'black'

\usepackage{lipsum}
% Used for inserting dummy 'Lorem ipsum' text into the template

\usepackage[scale=0.75]{geometry}
% Reduce document margins

\setlength{\hintscolumnwidth}{3.8cm}
% Uncomment to change the width of the dates column; overridden in the list of publications

\setlength{\makecvtitlenamewidth}{10cm}
% For the 'classic' style, uncomment to adjust the width of the space allocated to your name

\usepackage[utf8]{inputenc} 
% For special characters input from keyboard

\nopagenumbers{}
% Comment for page numbering

\usepackage{bm}
% For bold in math environment

\usepackage{ragged2e}
% For justification

\renewcommand{\listitemsymbol}{$\bullet$~}
% Changes the symbol used for lists

\makeatletter
\g@addto@macro\bfseries{\boldmath}
\makeatother
% For bold maths in titles

\AfterPreamble{\hypersetup{
  colorlinks=true,
  urlcolor=blue,
}}
% For interactive hyperlinks
